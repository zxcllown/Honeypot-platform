[build-system]
requires = ["setuptools>=64.0"]
build-backend = "setuptools.build_meta"

[project]
name = "honeypot-shared"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "redis>=5.0",
    "pydantic>=2.0",
]